rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /links/{linkId} {
      allow read: if true;
      allow write: if true; // Hoặc thêm điều kiện bảo mật của bạn
    }
  }
}
